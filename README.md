# Primera aplicacion con React Vite

Este proyecto se inicio con [Create Vite](https://vitejs.dev/guide/#scaffolding-your-first-vite-project).

## Scripts Disponibles

In the project directory, you can run:

### `npm run dev`

Ejecuta la aplicación en el modo de desarrollo.\
Abre [http://localhost:3000](http://localhost:3000) en su navegador para poder visualizar la aplicacion en el servidor local.

### `npm run build`

Crea la aplicación para la producción en la carpeta `dist`.\
Empaqueta correctamente React en modo de producción y optimiza la compilación para obtener el mejor rendimiento

### `npm run preview`

Ejecuta la aplicación compilada con `npm run build` en la carperta `dist` para obtener una vista previa localmente .\

## Construccion de App

### API GIPHY

En la aplicación se utilizo API de [Giphy](https://developers.giphy.com/docs/api/endpoint#search-endpoint) para obtener los gifs.
